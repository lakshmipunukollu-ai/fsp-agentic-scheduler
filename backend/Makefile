# Convenience targets when your shell is already in backend/
# Full repo workflow (backend + frontend install) lives in ../Makefile
.PHONY: install dev migrate seed test build

install:
	npm install

dev:
	npm run dev

migrate:
	npm run migrate

seed:
	npm run seed

test:
	npm test

build:
	npm run build
